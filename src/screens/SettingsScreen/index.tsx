import {FC, ReactElement} from 'react';

export type SettingsScreenProps = {};

const SettingsScreen: FC<SettingsScreenProps> = (): ReactElement => {
  return <div></div>;
};

export default SettingsScreen;
